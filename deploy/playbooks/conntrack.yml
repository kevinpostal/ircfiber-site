---
- name: Tune nf_conntrack for high-connection Docker/IRC workloads
  hosts: ircfiber
  become: true
  gather_facts: true
  vars_files:
    - "{{ playbook_dir }}/../inventories/production/group_vars/vault.yml"
  roles:
    - role: conntrack
