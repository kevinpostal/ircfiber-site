diff --git a/modules/nickserv/nickserv.cpp b/modules/nickserv/nickserv.cpp
index 4adf5dfbc..e821d8187 100644
--- a/modules/nickserv/nickserv.cpp
+++ b/modules/nickserv/nickserv.cpp
@@ -486,7 +486,8 @@ public:
 		{
 			if (u->server == s)
 			{
-				if (u->HasMode("REGISTERED") && !u->IsIdentified(true))
+				if (u->HasMode("REGISTERED") && !u->IsIdentified(true)
+					&& Config->GetModule(this).Get<bool>("deauthunknown", "yes"))
 					u->RemoveMode(NickServ, "REGISTERED");
 				if (!u->IsIdentified())
 					this->Validate(u);
@@ -501,7 +502,8 @@ public:
 		if (!na || na->nc != u->Account())
 		{
 			/* Remove +r, but keep an account associated with the user */
-			u->RemoveMode(NickServ, "REGISTERED");
+			if (Config->GetModule(this).Get<bool>("deauthunknown", "yes"))
+				u->RemoveMode(NickServ, "REGISTERED");
 
 			this->Validate(u);
 		}
diff --git a/modules/protocol/inspircd.cpp b/modules/protocol/inspircd.cpp
index c6abdc12e..79a0e1b3f 100644
--- a/modules/protocol/inspircd.cpp
+++ b/modules/protocol/inspircd.cpp
@@ -1823,8 +1823,12 @@ private:
 			auto *na = NickAlias::Find(value);
 			if (!na)
 			{
-				// Nick has been dropped, force the IRCd to deauth them.
-				IRCD->SendLogout(u);
+				// Nick has been dropped, force the IRCd to deauth them —
+				// but only when this instance is the network's account
+				// authority. A bridge-only Anope whose database holds just
+				// its own service accounts must never log the network out.
+				if (Config->GetModule("nickserv").Get<bool>("deauthunknown", "yes"))
+					IRCD->SendLogout(u);
 				return;
 			}
 
