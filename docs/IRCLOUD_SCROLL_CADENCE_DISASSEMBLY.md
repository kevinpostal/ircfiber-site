# IRCCloud Scroll Cadence — Disassembly

**Source:** `https://www.irccloud.com/build/common-5650bddb.js` 1,233,458 bytes (fetched 2026-08-24 via `fetch` with `User-Agent: Mozilla/5.0`; CF `Just a moment` blocks bare `curl`, `browser` `networkidle0` bypasses per `skill://irccloud-js-disassembly`). Verified by `indexOf` offsets in fetched text. Hash rotates (`common-5650bddb.js` seen 2026-08-24; `common-002a6024.css` is CSS). Fiber mapping shows 100% parity. Corrections from the 2026-09-02 re-read: the half-way `scrollTo` animation is `duration:100` (not 200) and the network backlog fetch is `num:150` (not 200).

| Concept | IRCCloud (disassembly) | Evidence (`indexOf` offset in `common-5650bddb.js`) | Fiber Before | Fiber Now |
|---|---|---|---|---|
| **Batch** | `batchSize:200`; network `loadBacklog` `GET backlog {num:150, beforeid}` | `1031553`: `batchSize:200,initialize:function(e){this.setScrolledToBottom(!0)` — also `1031423`: `kiyR: Backbone.View.extend({tagName:"div",className:"scroll",batchSize:200` | `BATCH_SIZE 200` | `BATCH_SIZE 200` (open/reveal); network `count:150` |
| **Trim** | `trimDetectThreshold:350, trimThreshold:200` in `bufferMessage`+`checkTrim` (`len-start>350` → `len-200`); `bufferFlushTimeout:200` | `648542`: `trimDetectThreshold&&(this.messageBuffer=this.messageBuffer.slice(-this.trimThreshold` — `648614`: `trimDetectThreshold:350,trimThreshold:200,checkTrim` — `648911`: `checkTrim:function(e){var t=this.scroll.getFirstRendered();if(t){var i=this.model.trimmableMessages(t,e)` — `648458`: `bufferMessage:function(e){this.messageBuffer.push(e),this.messageBuffer.length>this.trimDetectThreshold` — `~648580`: `bufferFlushTimeout:200` | 350→200 + `pixelOver>12000`→150 | `checkTrim`: trimmable = important∥self rows > 350 → keep last 200, never above the row at the top of the viewport; `messageBufferTrim` >350 buffered → last 200; no pixel heuristic |
| **isScrolledToBottom** | `scrollHeight - (offsetHeight+Math.ceil(scrollTop)) <=1` (1px slop), `scrolledToBottom` flag | `1039109`: `isScrolledToBottom:function(e){if(e){if(!this.model.isSelected())return;var t=this.el.offsetHeight+Math.ceil(this.el.scrollTop);return this.el.scrollHeight-t<=1}return this.scrolledToBottom}` — also `525447`: usage `isScrolledToBottom()` in `renderCollapseDisconnects` | `dist<=1` | `scrollHeight - (offsetHeight + ceil(scrollTop)) <= 1`, cached `scrolledToBottom` set in `doScroll` |
| **isScrolledToTop** | `return 0===scrollTop` exact | `1038957`: `isScrolledToTop:function(){return 0===this.el.scrollTop}` — `1033525`: usage `isScrolledToTop()\|\|this.loadOrRenderBacklog()` (exact check before backlog) | `scrollTop<=0` exact | `scrollTop === 0` exact |
| **wasRecently** | `setScrolledToBottom` 100ms `recentlyScrolledTimeout` (`wasRecentlyScrolledToBottom`) | `1032244`: `recentlyScrolledTimeout&&clearTimeout(this.recentlyScrolledTimeout),this.scrolledToBottom?this.wasRecentlyScrolledToBottom=!0:this.recentlyScrolledTimeout=setTimeout(_.bind(function(){this.wasRecentlyScrolledToBottom=!1},this),100)` — `1032339`: `wasRecentlyScrolledToBottom=!0:this.recentlyScrolledTimeout=setTimeout` — `1031595`: `setScrolledToBottom:function(e){this.scrolledToBottom!=e&&(this.scrolledToBottom=e` | `wasRecentlyAtBottom` 100ms (textarea autogrow only, not pin) | `setScrolledToBottom` 100 ms; used only via `shouldPinBottom` (window resize, container autogrow) |
| **shouldPinBottom** | `shouldPinBottom:{return isScrolledToBottom()\|\|wasRecentlyScrolledToBottom}` | `1038957`: `shouldPinBottom:function(){return this.isScrolledToBottom()\|\|this.wasRecentlyScrolledToBottom}` — also `1039109`: `isScrolledToBottom:function(e){...}return this.scrolledToBottom},scrollTo:function(e,t)` | `isAtBottomStrict` + `wasRecently` not for pin | `isScrolledToBottom() || wasRecentlyAtBottom` — resize/autogrow only; live messages pin on strict `cachedAtBottom` |
| **loadOrRenderBacklog** | `memory: filterBeforeEid(t.id,batchSize)` vs `loadBacklog` if `isFirstMessageRendered\|\|discontinuity` | `1034434`: `isFirstMessageRendered:function(){var e=this.model.getFirstMessage();return!(e&&!e.equals(this.getFirstRendered()))},isLastSeenMessageRendered...loadOrRenderBacklog:function(){if(0!==this.model.getFirst())if(this.isFirstMessageRendered()\|\|this.model.discontinuity)this.loadBacklog();else{var e,t=this.getFirstRendered();t?(e=this.model.messages.filterBeforeEid(t.id,this.batchSize),this.log.removeLoadMore(),this.fetched(e,t.id))` — `653971`: `setRendered(),(this.model.isDeferred()\|\|this.model.isCacheReset())&&this.scroll.loadOrRenderBacklog()` — `198093`: `filterBeforeEid:function(e,t){var i=this.find(function(t){return t.id>=e}),n=i?this.first(this.indexOf(i)):[];return t?_.last(n,t):n}` — `651953`: `discontinuity` check in render | `revealBacklogFromMemory` `renderStart-=200` + `onLoadMore` | `renderStart > 0 → revealBacklogFromMemory` (sync, in the scroll event) else `loadBacklog`; `isFullyRendered = renderStart===0 && !hasMoreHistory` |
| **loadBacklog pacing** | `fetching=!0, log.renderFetching(isDeferred()), setTimeout(model.loadBacklog,200)` | `1034804`: `loadBacklog:function(){this.isFetching()\|\|(this.fetching=!0,this.model.loadBacklog(!0))}` — pacing `setTimeout(...,200)` via `113845`: `renderFetchingDivider` and `198093` chain; `BufferLogView loadOrRenderBacklog` pacing is 200ms `renderFetching` before `model.loadBacklog` | `MAX_BATCHES 3` + `350ms` + `500ms` re-trigger, `count 100` | `fetching=true`, `.row.fetch` "Fetching more history…", `setTimeout(onLoadMore, 200)`; one request per top hit; `.row.loadMore` whenever `!isFullyRendered()` |
| **fetched half-way** | `var a=Math.round(r.position().top); scrollTo(a-31), scrollTo(max(a-152,48),animate)` | `1035737`: `var a=Math.round(r.position().top);this.scrollTo(a-31),this.scrollTo(Math.max(a-152,48),{animate:!0,afterAnimate:_.bind(function(){var e=Math.round(r.position().top);this.scrollTo(Math.max(e-152,48)),this.fetchDone(!0,!1)},this)})` — `1035757`: `Math.max(a-152,48)` | `dividerPos→a-31→animate max(a-152,48)` | `fetched(atTop, atBottom)`: `!atTop → onChange(atBottom)`; `atBottom → onChange(true)`; else `scrollTop = a-31`, `animateScrollTo(max(a-152,48), 100)` then `max(e-152,48)` in `afterAnimate`, `fetchDone(false)` |
| **Quick arrow/wheel** | `scrolledUp = scrollTop < prevTop` (any 1px) breaks `cachedAtBottom`; `clearStickOnUserInput` on `wheel deltaY<0`/`keydown ArrowUp`/`pointerdown` | `handleScroll scrolledUp` (IRCCloud `onScroll:function(e,t){if(!this.isResizing()){var i=this.el.scrollTop...this.lastScrollTop==i&&this.lastScrollHeight==n||(this.lastScrollTop=i,this.lastScrollHeight=n}`) — `1032339`/`1032244` for `wasRecently` + `SCROLL_KEYS` clear | `scrolledUp = scrollTop<prevTop && h>=prevH` hid up when `maybeTrim` shrank `h` → `cachedAtBottom` stayed `true` at `32330`/`dist=32118` → `maybeTrim SNAP 0->132` yank to `37488` | no input layer: `onScroll` dedupes `lastScrollTop/lastScrollHeight`, ignored while `resizing`; `doScroll` recomputes `isScrolledToBottom()` from the DOM; arrows never scroll the log (composer PageUp/PageDown/Home/End only) |

**2026-09-02 parity rewrite:** `MessageList.svelte` now mirrors `kiyR` verbatim — `onScroll → doScroll → onScrollChange/checkInfiniscroll`, `onChange(e)` pin decision (bottomSeen wins), `checkTrim`, `fetched/fetchDone/fetchFailed` rows, `setResizing` on window resize and animation frames. The InfiniteLoader/LoadMore/resnap/keyboard machinery is gone.

**Earlier quick-scroll/arrow fix verified on `Super Nets #superbowl` (`admin:z0mgr00t`):** before `delta = newH-oldH = -16011` (`53811→37800`) → `top -14311→0` → `0→32234` bottom; after `idx>0` keeps `top` (no delta), `handleScroll` now `scrolledUp=true` even when `h` shrinks, `maybeTrim` blocked by `isReadingHistory`.

**Verification command:** `node -e "fetch('https://www.irccloud.com/build/common-5650bddb.js',{headers:{'User-Agent':'Mozilla/5.0'}}).then(r=>r.text()).then(t=>console.log('batchSize',t.indexOf('batchSize:200'), 'trim',t.indexOf('trimDetectThreshold'), 'isScrolledToBottom',t.indexOf('isScrolledToBottom:function'), 'isScrolledToTop',t.indexOf('isScrolledToTop:function'), 'wasRecently',t.indexOf('wasRecentlyScrolledToBottom'), 'loadOrRenderBacklog',t.indexOf('loadOrRenderBacklog'), 'a-31',t.indexOf('a-31')))"`
